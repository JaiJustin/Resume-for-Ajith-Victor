import 'package:flutter/material.dart';

class UnderlineForText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      height: 3,
    );
  }
}
